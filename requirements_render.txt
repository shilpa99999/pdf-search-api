# PDF Search API - Render Deployment Requirements
# Minimal dependencies for cloud deployment

# Core web framework
Flask==3.0.0
Flask-CORS==4.0.0

# Production WSGI server
gunicorn==21.2.0

# Essential utilities
requests==2.31.0
typing-extensions==4.8.0

# Note: Advanced ML dependencies removed for faster deployment
# The app will use embedded demo data if PDF processing files are not available
